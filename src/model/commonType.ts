export interface TExternalUrls {
  spotipy: string;
}

export interface TImage {
  url: string;
  height: number | null;
  width: number | null;
}
export interface TRestrictions {
  reason?: string;
}
